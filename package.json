{
  "name": "vue-react-mfe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --names \"LAYOUT,REMOTE,REACT\" --prefix-colors \"blue,green,yellow\" \"pnpm --filter layout dev\" \"pnpm --filter remote remote\" \"pnpm --filter remote-react remote\"",
    "dev:layout": "pnpm --filter layout dev",
    "dev:remote": "pnpm --filter remote remote",
    "dev:remote-react": "pnpm --filter remote-react remote",
    "build": "pnpm --filter layout build && pnpm --filter remote build && pnpm --filter remote-react build",
    "build:layout": "pnpm --filter layout build",
    "build:remote": "pnpm --filter remote build",
    "build:remote-react": "pnpm --filter remote-react build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
} 
